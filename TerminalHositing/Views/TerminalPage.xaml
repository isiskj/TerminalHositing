<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TerminalHositing.Views.TerminalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TerminalHositing.Views"
    xmlns:vm="using:TerminalHositing.ViewModels"
    xmlns:m="using:TerminalHositing.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded">
    <!--<Page.DataContext>
        <vm:MainViewModel/>
    </Page.DataContext>-->

    <!--<Grid>
        <TreeView ItemsSource="{Binding Drives}">
            <TreeView.ItemTemplate>
                <DataTemplate x:DataType="m:DirectoryItem">
                    <TreeViewItem ItemsSource="{Binding SubDirectories}" Content="{Binding Name}"/>
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>-->
</Page>
